### Setup:

```
git clone
cd
bundle install
```

> Test the app with:
```
ruby lib/app.rb
```

> Run live specs with:
```
RUN_SPEC=true bundle exec guard
```

> Run live rubocop with:
```
RUN_RUBOCOP=true bundle exec guard
```
